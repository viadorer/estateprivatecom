# Backend Environment Variables
PORT=3001
NODE_ENV=development

# Database
DATABASE_PATH=./realestate.db

# JWT Authentication (POVINNE!)
# Vygeneruj: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-min-32-characters-random
JWT_REFRESH_SECRET=your-refresh-secret-key-min-32-characters-random

# Email (pro notifikace a reset hesla)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=Estate Private <noreply@estateprivate.com>

# URLs
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:3001
IMPORT_API_URL=http://localhost:3001

# Produkční URLs (pro deployment)
# FRONTEND_URL=https://estateprivate.com
# API_URL=https://api.estateprivate.com
# IMPORT_API_URL=https://import.estateprivate.com
